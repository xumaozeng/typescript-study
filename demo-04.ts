/**
 * type annotation 类型注解
 * type inference 类型推断
 */

let count1: number;
count1 = 123;

let countInference = 1233;

function getTotal(one: number, two: number) {
  return one + two;
}

const total = getTotal(1, 3);

const Xiaojiejie = {
  name: "刘莹",
  age: 18
};
